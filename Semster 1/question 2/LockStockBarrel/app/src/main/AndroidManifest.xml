<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="a15009851.dylanmatthews.lockstockbarrel">

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <uses-permission android:name="android.permission.SEND_SMS" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-permission android:name="android.permission.SEND_SMS" />

        <activity android:name=".activity_searchBY" />
        <activity android:name=".searchName" />
        <activity android:name=".searchTicketNumber" />
        <activity android:name=".searchDate" />
        <activity android:name=".searchNumber" />
        <activity android:name=".searchAll" />
        <activity android:name=".ViewResults" />
        <activity android:name=".completedRepairs" />
        <activity android:name=".insertRepair" />
        <activity android:name=".searchByCompletedRepairs" />
        <activity android:name=".updateRepair"></activity>
    </application>

</manifest>